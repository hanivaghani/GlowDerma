<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>{{title}}</h1>
    </header>

    <main>
        <section class="testimonial-grid">
            {{#each testimonials}}
            <div class="testimonial-card">
                <h3>{{name}}</h3>
                <p><strong>Rating:</strong> <span class="stars">{{renderStars rating}}</span> ({{rating}}/5)</p>
                <p>"{{comment}}"</p>
                <p><em>{{formatDate date}}</em></p>
            </div>
            {{/each}}
        </section>
    </main>

    <footer>
        <p>&copy; 2025 Your Company. All rights reserved.</p>
    </footer>
</body>
</html>